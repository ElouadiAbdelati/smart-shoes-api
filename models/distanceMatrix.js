class DistanceMatrix {
    constructor(duartionText,durationValue,distanceText,distanceValue,change,maneuver,distanceTextToEnd,durationTextToEnd,distanceValueToEnd,durationValueToEnd){
            this.maneuver = maneuver;
            this.duartionText = duartionText;
            this.durationValue = durationValue;
            this.distanceText = distanceText;
            this.distanceValue = distanceValue;
            this.change = change;
            this.distanceTextToEnd = distanceTextToEnd;
            this.durationTextToEnd = durationTextToEnd;
            this.distanceValueToEnd = distanceValueToEnd;
            this.durationValueToEnd = durationValueToEnd;
    }
}
module.exports = DistanceMatrix;